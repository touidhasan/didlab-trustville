# dependencies
node_modules/

# secrets — never commit
.env
.env.*
!.env.example
*.key
keystore/

# foundry
contracts/out/
contracts/cache/
contracts/lib/
contracts/broadcast/*/31337/
contracts/broadcast/**/dry-run/

# misc
.DS_Store
*.log
.vite/
# NOTE: dist/ is committed on purpose — cPanel publishes it.
services/rain-reporter/keys/

# Foundry run artifacts and the local anvil deployment — regenerated, never shared
contracts/broadcast/
deployments/31337.json
